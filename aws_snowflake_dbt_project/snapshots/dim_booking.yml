snapshots:
  - name: dim_booking
    relation: ref('booking')
    config:
        schema: gold
        database: Airbnb
        unique_key: booking_id
        strategy: timestamp
        updated_at: created_at
        dbt_valid_to_current: "to_date('9999-12-31')"